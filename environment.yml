name: symbolic-ics-artifact
channels:
  - conda-forge
dependencies:
  - python=3.11
  - numpy
  - pandas
  - scikit-learn
  - matplotlib
  - pytest
  - omegaconf
  - pyyaml
  - sympy
  - pip
  - pip:
      - -e .[dev,plot]
      # For full PySR discovery reruns, also run:
      # python -m pip install -r requirements-full.txt
